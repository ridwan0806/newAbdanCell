package com.example.abdancell.ViewHolder;

import android.view.View;

import androidx.annotation.NonNull;
import androidx.recyclerview.widget.RecyclerView;

public class VHListItem extends RecyclerView.ViewHolder implements View.OnClickListener{

    public VHListItem(@NonNull View itemView) {
        super(itemView);
    }

    @Override
    public void onClick(View view) {

    }
}
